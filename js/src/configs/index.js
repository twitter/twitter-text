// These json files are created by the build script
import version1 from './v1.json';
import version2 from './v2.json';

const defaults = version2;

export default {
  defaults,
  version1,
  version2
}
